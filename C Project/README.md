# TILE GAME
## 실행 환경

- MacOS

## 실행 방법

- **반드시 터미널(Terminal)에서 실행할 것.** Xcode 나 VS Code 의 내장 콘솔은 진짜 터미널이 아니라서 키 입력과 화면 출력이 정상 동작하지 않는다.
- Linux 에서는 `cc` 대신 `gcc` 를 써도 된다.
- 실행하면 같은 폴더에 `ranking.txt` 가 자동으로 만들어진다. 미리 준비할 파일은 없다.


